import { Settings } from 'styled-icons/material/Settings'
import { IconButton } from './VolumeButton'

const SettingsButton = () => (
  <IconButton width={4} size={2}>
    <Settings />
  </IconButton>
)

export default SettingsButton
